export const BreadcrumbItems = () => {
    return (
        <nav>
            <a href="#">main</a>
            <span>{` > `}</span>
            <a href="#">config</a>
            <span>{` > `}</span>
            <a href="#">mypage</a>
        </nav>
    )
}